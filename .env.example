# Your domain (used for both frontend and backend — same domain, path-based routing)
HOST=yourdomain.com

# Backend runtime config
ALLOWED_ORIGIN=https://yourdomain.com
JWT_SECRET=change_me_use_openssl_rand_hex_32
LOG_LEVEL=info
